const exec = require('child_process');

exec('ssh -i .ssh/nededge_dsa -q root@192.168.126.3 wstalist', (error, stdout, stderr) => {
  if (error) {
    console.error(`exec error: ${error}`);
    return;
  }
  console.log(`stdout: ${stdout}`);
  console.log(`stderr: ${stderr}`);
});
